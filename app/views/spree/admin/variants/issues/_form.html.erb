<h3><%= t(:issue) %></h3>
<%= f.fields_for :issues do |builder| %>
  <%= render 'issue_fields', :f => builder %>  
<% end %>

<p class="form-buttons" data-hook="buttons">
  <%= button t(:update) %>
</p>
